

class User {

  constructor(uid, psw, first_name, last_name, email) {
    this.uid = uid
    this.psw = psw
    this.first_name = first_name
    this.last_name = last_name
    this.email = email
  }

}

module.exports = User
